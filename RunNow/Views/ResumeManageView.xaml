<UserControl x:Class="RunNow.Views.ResumeManageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"

            xmlns:local="clr-namespace:RunNow.Views"
             mc:Ignorable="d">

    <Grid Background="#00478F">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,30,0,20" HorizontalAlignment="Center">
            <TextBlock Text="이력서 관리 ✈️"
                       FontSize="20" FontWeight="Bold" Foreground="White"
                       HorizontalAlignment="Center"/>
        </StackPanel>

        <Border Grid.Row="1" Margin="30,0,30,10" Padding="20" CornerRadius="30" Background="White">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <StackPanel HorizontalAlignment="Center">
                        <Ellipse Width="80" Height="80" Stroke="#CCC" StrokeThickness="1" Margin="0,10">
                            <Ellipse.Fill>
                                <ImageBrush ImageSource="/Assets/user.png" Stretch="UniformToFill"/>
                            </Ellipse.Fill>
                        </Ellipse>
                    </StackPanel>

                    <StackPanel Margin="10" Orientation="Vertical">
                        <TextBlock Text="이름" FontWeight="Bold"/>
                        <TextBox Text="{Binding Name}" Margin="0,3,0,10" Height="25"/>

                        <TextBlock Text="생년월일" FontWeight="Bold"/>
                        <StackPanel Orientation="Horizontal" Margin="0,3,0,10">
                            <ComboBox ItemsSource="{Binding Years}" SelectedItem="{Binding SelectedYear}" Width="80" Margin="0,0,10,0"/>
                            <ComboBox ItemsSource="{Binding Months}" SelectedItem="{Binding SelectedMonth}" Width="60" Margin="0,0,10,0"/>
                            <ComboBox ItemsSource="{Binding Days}" SelectedItem="{Binding SelectedDay}" Width="60"/>
                        </StackPanel>

                        <TextBlock Text="E-mail" FontWeight="Bold"/>
                        <StackPanel Orientation="Horizontal" Margin="0,3,0,10">
                            <TextBox Text="{Binding EmailId}" Width="150" Height="25"/>
                            <TextBlock Text="@" VerticalAlignment="Center" Margin="5,0"/>
                            <ComboBox ItemsSource="{Binding EmailDomains}" SelectedItem="{Binding SelectedEmailDomain}" Width="150" Height="30"/>
                        </StackPanel>

                        <TextBlock Text="전화번호" FontWeight="Bold"/>
                        <StackPanel Orientation="Horizontal" Margin="0,3,0,10">
                            <ComboBox ItemsSource="{Binding PhonePrefixes}" SelectedItem="{Binding SelectedPhonePrefix}" Width="70"/>
                            <TextBlock Text="-" Margin="5,0"/>
                            <TextBox Text="{Binding PhoneMid}" Width="80" MaxLength="4"/>
                            <TextBlock Text="-" Margin="5,0"/>
                            <TextBox Text="{Binding PhoneEnd}" Width="80" MaxLength="4"/>
                        </StackPanel>

                        <TextBlock Text="주소" FontWeight="Bold"/>
                        <TextBox Text="{Binding Address}" Margin="0,3,0,10" Height="25"/>
                    </StackPanel>

                    <Border BorderBrush="#999" BorderThickness="1" CornerRadius="10" Margin="0,20,0,10">
                        <StackPanel Margin="10">
                            <TextBlock Text="경력사항" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBox Text="{Binding Career}" AcceptsReturn="True" Height="80"/>
                        </StackPanel>
                    </Border>

                    <Border BorderBrush="#999" BorderThickness="1" CornerRadius="10" Margin="0,10,0,10">
                        <StackPanel Margin="10">
                            <TextBlock Text="자격사항" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <TextBox Text="{Binding SelectedCertificate}" Width="300" IsReadOnly="True"/>
                                <Button Content="🔍" Width="30" Height="30" Command="{Binding OpenCertificateCommand}"/>
                            </StackPanel>

                            <Border BorderBrush="#ccc" BorderThickness="1" CornerRadius="5" Margin="0,10,0,0" Padding="5">
                                <ListBox ItemsSource="{Binding Certificates}">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" Margin="2">
                                                <TextBlock Text="{Binding}" Margin="0,0,5,0"/>
                                                <Button Content="❌"
                                                        Command="{Binding DataContext.RemoveCertificateCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                        CommandParameter="{Binding}"
                                                        Background="Transparent"
                                                        BorderThickness="0"
                                                        Width="20" Height="20"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </Border>
                        </StackPanel>
                    </Border>

                    <Border BorderBrush="#999" BorderThickness="1" CornerRadius="10" Margin="0,10,0,10">
                        <StackPanel Margin="10">
                            <TextBlock Text="특이사항" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBox Text="{Binding Notes}" AcceptsReturn="True" Height="80"/>
                        </StackPanel>
                    </Border>

                    <Button Content="이력서 저장"
                            Background="#00478F"
                            Foreground="White"
                            FontWeight="Bold"
                            Height="40"
                            Margin="0,10,0,20"
                            Command="{Binding SendToServerCommand}"/>
                </StackPanel>
            </ScrollViewer>
        </Border>
        <local:BottomNav Grid.Row="3"/>
    </Grid>
</UserControl>
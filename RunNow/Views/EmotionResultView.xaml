<UserControl x:Class="RunNow.Views.EmotionResultView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Background="#F2F2F2">

    <Grid>
        <!-- 상단 타이틀 -->
        <TextBlock Text="감정분석결과"
                   FontFamily="Arial Black"
                   FontSize="40"
                   FontWeight="Bold"
                   Foreground="#00478F"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Top"
                   Margin="0,27,0,0"
                   Width="400" Height="45"/>

        <Grid Margin="0,70,0,0">
            <Border BorderBrush="Black" BorderThickness="3" Margin="20" CornerRadius="30" Padding="20">
                <StackPanel>
                    <!-- 범례 -->
                    <Grid Height="30" Margin="100,0,100,0">
                        <Border Background="White" BorderBrush="Black" BorderThickness="1" CornerRadius="5">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Rectangle Grid.Column="0" Fill="Red"/>
                                <Rectangle Grid.Column="1" Fill="Orange"/>
                                <Rectangle Grid.Column="2" Fill="Gold"/>
                                <Rectangle Grid.Column="3" Fill="LimeGreen"/>

                                <TextBlock Text="극심한" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
                                <TextBlock Text="심한" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
                                <TextBlock Text="보통" Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
                                <TextBlock Text="좋음" Grid.Column="3" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
                            </Grid>
                        </Border>
                    </Grid>

                    <Path Stroke="Black" StrokeThickness="2" Data="M 0,10 L 120,10 L 110,0 M 120,10 L 110,20" HorizontalAlignment="Center" Margin="0,10"/>
                    <TextBlock Text="영역   상태" FontSize="30" FontWeight="Bold" Foreground="SlateGray" HorizontalAlignment="Center"/>

                    <!-- 결과 바인딩 -->
                    <StackPanel Margin="30" HorizontalAlignment="Center" Width="500">
                        <ItemsControl ItemsSource="{Binding EmotionResults}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="300"/>
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Text="{Binding Category}" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>

                                        <Grid Grid.Column="1" Height="18">
                                            <Border Background="White" BorderBrush="Black" BorderThickness="1" CornerRadius="5">
                                                <Rectangle Fill="{Binding BarColor}"
                                                           Width="{Binding BarWidth}"
                                                           Height="18"
                                                           RadiusX="5" RadiusY="5"/>
                                            </Border>
                                        </Grid>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- 진단 메시지 -->
                        <TextBlock Text="진단 메시지" FontSize="30" FontWeight="Bold" Foreground="SlateGray" Margin="0,20,0,30" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding RandomDiagnosis}" FontSize="20" TextWrapping="Wrap" Foreground="SlateGray" TextAlignment="Center" Width="450"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>

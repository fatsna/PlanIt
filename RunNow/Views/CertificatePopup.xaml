<Window x:Class="RunNow.Views.CertificatePopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:RunNow.Converters"
        Title="자격증 검색" Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True">

    <Window.Resources>
        <conv:StringNullOrEmptyToVisibilityConverter x:Key="StringNullOrEmptyToVisibilityConverter"/>
    </Window.Resources>

    <StackPanel Margin="10">
        <!-- Search with Placeholder -->
        <Grid Margin="0,0,0,10">
            <TextBox Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}" FontSize="14" />
            <TextBlock Text="자격증 검색"
                       Foreground="Gray"
                       Margin="5,0,0,0"
                       VerticalAlignment="Center"
                       IsHitTestVisible="False"
                       Visibility="{Binding SearchKeyword, Converter={StaticResource StringNullOrEmptyToVisibilityConverter}}"/>
        </Grid>

        <ListBox ItemsSource="{Binding FilteredCertificates}"
                 SelectedItem="{Binding SelectedCertificate}"
                 Height="180"
                 DisplayMemberPath="name"/>

        <Button Content="선택"
                HorizontalAlignment="Right"
                Width="80"
                Margin="0,10,0,0"
                Command="{Binding ConfirmCommand}"/>
    </StackPanel>
</Window>

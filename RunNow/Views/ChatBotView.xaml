<UserControl x:Class="RunNow.Views.ChatBotView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:RunNow.Views"
             xmlns:converters="clr-namespace:RunNow.Converters"
             x:Name="Root"
             Background="White">

    <UserControl.Resources>
        <converters:BoolToAlignmentConverter x:Key="BoolToAlignmentConverter"/>
        <converters:BoolToColorConverter x:Key="BoolToColorConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:BoolToVisibilityInverseConverter x:Key="BoolToVisibilityInverseConverter"/>
        <converters:BoolToTextAlignmentConverter x:Key="BoolToTextAlignmentConverter"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="150"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#004AAD">
            <Grid>
                <Image Source="/Assets/LOGO2.png" Height="90"
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Options -->
        <ItemsControl ItemsSource="{Binding Options}" Grid.Row="1">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel HorizontalAlignment="Center"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding}"
                            Command="{Binding DataContext.SelectOptionCommand, Source={x:Reference Root}}"
                            CommandParameter="{Binding}"
                            Margin="5" Padding="10,5" FontSize="12" MinWidth="80" MaxWidth="200">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border BorderBrush="#004AAD" BorderThickness="1" CornerRadius="20" Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#E3F2FD"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Chat -->
        <ListBox x:Name="ChatListBox"
                 ItemsSource="{Binding Messages}"
                 Grid.Row="2"
                 BorderThickness="0"
                 Background="Transparent"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 HorizontalContentAlignment="Stretch">

            <!-- ✅ 리스트 아이템 hover/선택 하이라이트 완전 제거 -->
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                    <Setter Property="Focusable" Value="False"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}">
                                    <ContentPresenter/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="{Binding IsUserMessage, Converter={StaticResource BoolToAlignmentConverter}}"
                                Margin="10">

                        <!-- 봇 프로필 (사용자 메시지일 때 숨김) -->
                        <Image Source="{Binding ProfileImage}"
                               Width="40" Height="40" Margin="5" Stretch="UniformToFill"
                               VerticalAlignment="Top"
                               RenderOptions.BitmapScalingMode="HighQuality"
                               Visibility="{Binding IsUserMessage, Converter={StaticResource BoolToVisibilityInverseConverter}}">
                            <Image.Clip>
                                <EllipseGeometry Center="20,20" RadiusX="20" RadiusY="20"/>
                            </Image.Clip>
                        </Image>

                        <!-- 말풍선 -->
                        <Border CornerRadius="15" Padding="10" Margin="5"
                                Background="{Binding IsUserMessage, Converter={StaticResource BoolToColorConverter}}"
                                MinWidth="120" MaxWidth="300">
                            <TextBlock Text="{Binding Message}"
                                       TextWrapping="Wrap"
                                       FontSize="15"
                                       TextAlignment="{Binding IsUserMessage, Converter={StaticResource BoolToTextAlignmentConverter}}"/>
                        </Border>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Input -->
        <Grid Grid.Row="3" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" CornerRadius="20" BorderBrush="Gray"
                    BorderThickness="1" Background="#F5F5F5" Padding="5">
                <TextBox Text="{Binding UserInput, UpdateSourceTrigger=PropertyChanged}"
                         Background="#F5F5F5"
                         BorderThickness="0"
                         FontSize="14"
                         VerticalContentAlignment="Center"
                         HorizontalAlignment="Stretch"
                         Height="30"
                         Margin="5,0">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding SendCommand}" />
                    </TextBox.InputBindings>
                </TextBox>
            </Border>

            <Button Grid.Column="1" Width="50" Height="50" Margin="10,0,0,0"
                    Background="#004AAD" BorderBrush="Transparent"
                    Command="{Binding SendCommand}" VerticalAlignment="Center">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse Fill="{TemplateBinding Background}"/>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                    </ControlTemplate>
                </Button.Template>
                <Image Source="/Assets/sendb.png" Width="40" Height="40" RenderOptions.BitmapScalingMode="HighQuality"/>
            </Button>
        </Grid>

        <local:BottomNav Grid.Row="4"/>
    </Grid>
</UserControl>
